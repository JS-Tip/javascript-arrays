const phrases = ['The', 'developer', 'writes', 'a', 'function'];
const phrasesSorted = phrases.sort();
// Gotcha - uppercase strings sorted before lowercase strings
console.log('phrases sorted', phrasesSorted);

const numbers = [2, 4, 1, 5, 3];
const numbersSorted = numbers.sort();
console.log('numbers sorted', numbersSorted);
